{
  "name": "spill-functions",
  "description": "Firebase Cloud Functions for Spill (admin create staff with emailVerified)",
  "main": "index.js",
  "engines": { "node": "18" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0"
  },
  "private": true
}
